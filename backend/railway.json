{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "prisma db push --skip-generate || echo 'Prisma db push failed; starting API anyway'; exec uvicorn app.entrypoint:app --host 0.0.0.0 --port 8080 --lifespan off"
  }
}